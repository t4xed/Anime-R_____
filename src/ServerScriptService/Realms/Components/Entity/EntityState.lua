local ReplicatedStorage = game:GetService("ReplicatedStorage")

local EntityState = {}

function EntityState:Construct(EntityComponent)
    
end

function EntityState:ShouldConstruct(EntityComponent)
    
end

return EntityState